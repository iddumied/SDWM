void drawtbartext();

void drawtbartext()
{/*
  int y,h,x;
  h = dc.font.ascent + dc.font.descent;
  y = dc.y + (dc.h / 2) - (h / 2) + dc.font.ascent;
  x = dc.x + (h / 2);
  if(dc.font.set)
    XmbDrawString(dpy, dc.drawable, dc.font.set, dc.gc, x, y, text, 4);
  else
    XDrawString(dpy, dc.drawable, dc.gc, x, y, text, 4);  

*/}
